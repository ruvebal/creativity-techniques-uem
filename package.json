{
	"name": "creativity-techniques-uem",
	"version": "0.1.0",
	"description": "Creativity Techniques — design-degree creativity methods — Universidad Europea Creative Campus teaching site",
	"private": true,
	"scripts": {
		"hydrate": "node scripts/hydrate-field-site.mjs",
		"media:rehydrate": "PROFIELD_PROJECT=tc node scripts/rehydrate-student-media.mjs",
		"verify:publication": "node scripts/verify-publication-safety.mjs",
		"postcss:build": "postcss docs/assets/css/tailwind.css -o docs/assets/css/tailwind-processed.css",
		"postcss:watch": "postcss docs/assets/css/tailwind.css -o docs/assets/css/tailwind-processed.css --watch",
		"predevelop": "bash scripts/kill-dev-ports.sh",
		"develop": "npm run hydrate && npm run media:rehydrate && npm run postcss:build && bundle exec jekyll serve --source docs --livereload --livereload-port ${LIVERELOAD_PORT:-35731} --config _config.yml --port ${PORT:-4003}",
		"prebuild": "npm run hydrate && npm run media:rehydrate",
		"build": "npm run postcss:build && bundle exec jekyll build --source docs --destination _site --config _config.yml && npm run verify:publication",
		"covers": "node scripts/fractal-architecture/generate-triangle-tile.mjs && node scripts/archive-covers/generate-archive-covers.mjs"
	},
	"devDependencies": {
		"@tailwindcss/typography": "^0.5.19",
		"autoprefixer": "^10.4.21",
		"cssnano": "^7.1.2",
		"js-yaml": "^4.3.2",
		"postcss": "^8.5.6",
		"postcss-cli": "^11.0.1",
		"tailwindcss": "^3.4.18"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/ruvebal/creativity-techniques-uem.git"
	},
	"author": "Rubén Vega Balbás, PhD — Creative Technologist & Developer",
	"license": "MIT"
}
